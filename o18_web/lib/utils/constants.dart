/// Define if the request is new one or already existing one
enum RequestSelection {
  existedRequest,
  newReques,
}

/// Define if the house is new one or already existing one
enum HouseSelection {
  existedHouse,
  newHouse,
}
